[package]
name = "kernel_bootstrap"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.5.0"
console_vga = { path = "../console_vga", default-features = false }
# TODO: Re-enable when dependencies are no_std compatible
# services_editor_vi = { path = "../services_editor_vi" }
# input_types = { path = "../input_types" }
# view_types = { path = "../view_types" }
# serde = { workspace = true, default-features = false, features = ["derive", "alloc"] }

[[bin]]
name = "kernel_bootstrap"
path = "src/main.rs"
