[package]
name = "kernel_bootstrap"
version = "0.1.0"
edition = "2021"

[lib]
name = "kernel_bootstrap_lib"
path = "src/lib.rs"

[dependencies]
limine = "0.5.0"
editor_core = { path = "../editor_core", default-features = false }

[target.'cfg(not(test))'.dependencies]
console_vga = { path = "../console_vga", default-features = false }

[[bin]]
name = "kernel_bootstrap"
path = "src/main.rs"
